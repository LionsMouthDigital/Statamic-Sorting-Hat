# global:
#   # The absolute path to the directory that holds your sites. `/Users/{{ username }}` by default.
#   sites_path:

site:
  # The path to your project relative to `global:sites_path`.
  dir:  my_site
  # The path to the root of your site. Use `./` if `site:dir` is your root.
  root: html
